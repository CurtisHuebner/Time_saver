{
  "manifest_version": 2,

  "name": "Time Saver",
  "description": "Bocks a specified set of sites during user specified periods of the week using a JSON formatted string.",
  "version": "0.0.1",

  "background": {
      "scripts":["redirect.js"]
  },
  "options_page": "options.html",
  "permissions": [
    "<all_urls>",
    "webRequest",
    "webRequestBlocking",
    "storage"
  ]
}
