{
  "manifest_version": 2,

  "name": "Time Saver",
  "description": "Block a specified set of sites during specified periods of the week using a JSON formatted string.",
  "version": "0.0.1",

  "background": {
      "scripts":["redirect.js"]
  },
  "options_page": "options.html",
  "permissions": [
    "<all_urls>",
    "webRequest",
    "webRequestBlocking",
    "storage"
  ]
}
